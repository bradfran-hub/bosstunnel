# Generate independent random secrets; do not use these placeholders.
BOSS_ADMIN_TOKEN=
BOSS_SECRET=
PUBLIC_BASE_URL=https://your-boss.example
BOSS_CUSTOMER_ACCOUNTS=true
# Exact immediate reverse-proxy IPs only, comma-separated. Leave empty without a proxy.
BOSS_TRUSTED_PROXY_IPS=
BOSS_JELLYFIN_OUTPUT=false
